// Stub component source file — Card.figma.ts has no `// source=` directive,
// so the resolver's sibling probe finds this file via the basename heuristic.
export const Card = (_props: { title?: string; children?: unknown }) => null
