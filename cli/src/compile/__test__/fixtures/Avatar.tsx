// Stub component source file — Avatar.figma.template.ts has no `// source=`
// directive, so the resolver's sibling probe finds this file via the
// `Avatar.figma.template.ts` → `Avatar` basename heuristic.
export const Avatar = (_props: { size?: number; src?: string }) => null
