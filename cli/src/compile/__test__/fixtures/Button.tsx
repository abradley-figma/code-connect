// Stub component source file — exists only so the Code Connect resolver
// has a real sibling to point at when running real_fixtures.test.ts. The
// fixture's `// source=./Button.tsx` directive resolves to this file.
export const Button = (_props: {
  variant?: string
  size?: string
  disabled?: boolean
  label?: string
  icon?: unknown
}) => null
